import React from 'react';
import styles from './styles/Category.module.css';

const Categories = () => (
  <div className={styles.button}>
    <button className={styles.btn} type="button">Check status</button>
  </div>
);

export default Categories;
